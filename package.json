{
  "name": "@punchlab/gsd-cli",
  "version": "0.1.0",
  "description": "Autonomous AI development pipeline CLI",
  "type": "module",
  "bin": { "gsd": "./bin/gsd.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "files": ["dist", "bin"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/punchlab-dev/gsd-cli"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
